
pushContinue.onclick = function() {
  ChangeForm(Final)
}

/*
chestSubmit.onclick=function(){
  let chestDropdown = chestDropdown.value
  let chestSets = chestSets.value
  let chestReps = chestReps.value
  let chestWeight = chestWeight.value
  // ATTENTION NEED TO FIX: let query = "INSERT into exercise(name, reps, sets) SELECT exercise_group_id, 'curls', 10, 3 FROM exercise_group EG INNER JOIN workout w ON EG.workout_id = w.workout_idWHERE user_id = 1 AND w.`date`= '04-13-2021';
}

req = Ajax("https://ormond.creighton.edu/courses/375/ajax-connection.php", "POST", "host=ormond.creighton.edu&user=" + netID + "&pass=" + pw + "&database=" + databaseSchema + "&query=" + query)

shouldersSubmit.onclick=function(){
  let shouldersDropdown = shouldersDropdown.value
  let shouldersSets = shouldersSets.value
  let shouldersReps = shouldersReps.value
  let shouldersWeight = shouldersWeight.value
  // ATTENTION NEED TO FIX: let query = "INSERT into exercise(name, reps, sets) SELECT exercise_group_id, 'curls', 10, 3 FROM exercise_group EG INNER JOIN workout w ON EG.workout_id = w.workout_idWHERE user_id = 1 AND w.`date`= '04-13-2021';
}

req = Ajax("https://ormond.creighton.edu/courses/375/ajax-connection.php", "POST", "host=ormond.creighton.edu&user=" + netID + "&pass=" + pw + "&database=" + databaseSchema + "&query=" + query)

tricepsDropdown.onclick=function(){
  let tricepsDropdown = tricepsDropdown.value
  let tricepsSets = tricepsSets.value
  let tricepsReps = tricepsReps.value
  let tricepsWeight = tricepsWeight.value
  // ATTENTION NEED TO FIX: let query = "INSERT into exercise(name, reps, sets) SELECT exercise_group_id, 'curls', 10, 3 FROM exercise_group EG INNER JOIN workout w ON EG.workout_id = w.workout_idWHERE user_id = 1 AND w.`date`= '04-13-2021';
}

req = Ajax("https://ormond.creighton.edu/courses/375/ajax-connection.php", "POST", "host=ormond.creighton.edu&user=" + netID + "&pass=" + pw + "&database=" + databaseSchema + "&query=" + query)
*/